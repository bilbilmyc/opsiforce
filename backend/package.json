{
  "name": "@opsiforce/backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "nest build",
    "start": "NODE_OPTIONS='--max-http-header-size=128000' yarn node dist/src/main.js",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "prettier": "prettier --write src/",
    "ts": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx ./db/migrate.ts",
    "db:studio": "drizzle-kit studio",
    "create-opsiforce-db": "kubectl run -n local psql-client --restart=Never -i --tty --rm --env PGPASSWORD=dbpass1 --image=postgres:16 -- psql -h local-pg-postgresql -d postgres -U admin -c \"CREATE DATABASE opsiforce;\" || :",
    "setup-minikube-storage": "minikube ssh -- 'sudo mkdir -p /workspace-data/projects && sudo chmod 777 /workspace-data /workspace-data/projects'",
    "build-agent-image": "sh -c 'TAG=opsiforce-agent:$(node -p \"require(\\\"../agent-config/agent-image-version.json\\\").version\") && minikube image ls | grep -q \"$TAG\" && echo \"Image $TAG already exists, skipping build\" || minikube image build -t $TAG -f docker/Dockerfile.agent --build-opt=build-arg=OPENCODE_CONFIG=agent-config/opencode.local.json ..'",
    "rebuild-agent-image": "minikube image build -t opsiforce-agent:$(node -p \"require('../agent-config/agent-image-version.json').version\") -f docker/Dockerfile.agent --build-opt=build-arg=OPENCODE_CONFIG=agent-config/opencode.local.json ..",
    "opsiforce-install-infra": "helm upgrade --install --wait --namespace local opsiforce-infra ../helm/opsiforce --set fullnameOverride=opsiforce --set storage.type=hostPath --set storage.hostPath=/workspace-data --set valkey.enabled=false --set agent.affinity=null",
    "create-bifrost-db": "kubectl run -n local psql-client-bf --restart=Never -i --tty --rm --env PGPASSWORD=dbpass1 --image=postgres:16 -- psql -h local-pg-postgresql -d postgres -U admin -c \"CREATE DATABASE bifrost;\" || :",
    "install-bifrost": "sh ../scripts/install-bifrost.sh",
    "mailgun:mock": "docker run --rm -t -p 8089:80 -e MAILGUN_API_KEY=key-example asedov/mailgun-mock",
    "tilt:up": "tilt up --stream=false --log-source=build --host=0.0.0.0",
    "tilt:down": "tilt down",
    "minikube-dev": "env-cmd -f ./local-envs.sh --use-shell 'yarn run setup-minikube-storage && yarn run build-agent-image && yarn run opsiforce-install-infra && yarn run create-opsiforce-db && yarn run create-bifrost-db && yarn run db:migrate && yarn run install-bifrost && concurrently -k -n db,mailgun,tilt \"yarn run db:studio\" \"yarn run mailgun:mock\" \"yarn run tilt:up\"'"
  },
  "dependencies": {
    "@bull-board/api": "^6.21.2",
    "@bull-board/fastify": "^6.21.2",
    "@bull-board/nestjs": "^6.21.2",
    "@fastify/compress": "^8.3.1",
    "@fastify/multipart": "^9.4.0",
    "@kubernetes/client-node": "^1.4.0",
    "@nestjs/bull-shared": "^11.0.4",
    "@nestjs/bullmq": "^11.0.4",
    "@nestjs/common": "^11.1.19",
    "@nestjs/config": "^4.0.4",
    "@nestjs/core": "^11.1.19",
    "@nestjs/platform-fastify": "^11.1.19",
    "bullmq": "^5.73.5",
    "drizzle-orm": "^0.45.2",
    "fastify": "^5.8.5",
    "form-data": "^4.0.5",
    "ioredis": "^5.10.1",
    "mailgun.js": "^13.0.0",
    "postgres": "^3.4.9",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2",
    "sqlite": "^5.1.1",
    "sqlite3": "^6.0.1",
    "tsx": "^4.21.0"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.19",
    "@types/node": "^22.0.0",
    "concurrently": "^9.2.1",
    "drizzle-kit": "^0.31.10",
    "env-cmd": "^11.0.0",
    "typescript": "^5.9.3"
  }
}
