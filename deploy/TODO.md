# 部署待办

更新日期：2026-09-14。

- [x] 部署、渲染不依赖 `--node-ip`，集群内服务使用 Service DNS 和端口。
- [x] HTTP 回源入口，移除自动自签名证书和强制 HTTPS 跳转；测试 NodePort 30080，生产 ClusterIP 8080。
- [x] 浏览器域名与集群服务地址分开，前端读取运行时域名配置，不再从节点 IP 拼接配置。
- [x] Windows / Linux 构建默认推送，仓库失败保留本地镜像，tar 仍为可选。
- [x] Bifrost 动态渠道模型目录、虚拟 Key 授权同步、缺 Key 自动补建、页面选择平台默认模型。
- [x] Code/DB 改为主站路径，集群内经 Service 名回源，保留项目访问权限校验。
- [x] 从 Bifrost 同步上下文规格，移除统一 32K 限制；显示思考、工具和压缩进度。
- [ ] 配置并实测公司 CDN、业务域名、证书、WebSocket/SSE 转发。
- [ ] 接入并验证公司自己的登录认证服务。
- [ ] 如需完整中文产品界面，为 Opsiforce 外层页面补充国际化。

用法与限制见 [部署说明](README.md)。勾选代码实现不代表公司 CDN 或生产验证已完成。
