# 部署待办

记录日期：2026-09-14。以下事项留待后续部署整理，本轮先处理模型管理后台的配置。

- [ ] 去除部署对 `--node-ip` 的依赖，使用 Kubernetes 部署时不要求指定节点 IP。
- [ ] 集群内服务通过 Service 名称和端口通信，不把节点 IP 写入服务连接配置。
- [ ] 公网 HTTPS 由公司已有 CDN 和证书体系处理，应用部署提供回源服务入口；整理 CDN 到集群入口的转发配置。
- [ ] 去除部署脚本自动生成节点 IP 自签名证书、强制跳转 HTTPS 的测试入口设计。
- [ ] 清理从节点 IP 拼接域名、应用地址和回调地址的逻辑，基础部署不要求额外填写回调参数；确有业务依赖的地址按公司实际接入方式单独处理。
- [ ] 精简部署用法：构建默认推送、同步第三方镜像、部署 YAML；保留 Windows 和 Linux 构建能力，导出镜像包仍为可选项。
