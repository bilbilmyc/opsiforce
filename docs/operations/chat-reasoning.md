# 对话思考与状态显示

## 修复内容

- 连续的思考、工具执行和后台完成通知合并为「思考与执行过程」区域。执行中默认展开，结束后默认收起；用户手动选择优先。所有正式文字输出（包括执行中途的说明和最终答复）、错误和重试提示留在区域外。按消息类型区分，不根据位置或文字内容推断是否属于思考。正式输出会断开过程分组，保持原始先后顺序。
- 整体展开／收起使用独立状态，加载更早历史记录时保留选择，区域内仍可逐项查看工具或思考详情。
- 模型已开始思考但正文尚未到达时，显示等待说明；没有正文的标题不提供空折叠按钮。收到实时思考片段后直接显示正文。
- 整个过程区域最高为 420px 或视口高度的一半，以较小值为准。标题栏和操作固定在滚动区外，内侧有明显的滚动条、边框和提示；滚动到边缘时不带动外层对话。
- 区域内的单段思考取消独立滚动，仅保留一个过程滚动区。鼠标移到区域外滚动整段对话。
- 活动状态复用时间线原有状态位置；终止状态放在与消息相同的居中列内，消除左侧错位与重复的 Working 提示。
- 补充思考、通知、模型切换及继续对话输入提示的中文词条，随 Opsiforce 语言开关切换。

仅展示服务端实际返回的思考内容。中途读取消息接口可能得到空字符串：实时片段通过 `session.reasoning.delta` 事件传递，思考结束后再持久化全文。不能仅凭中途快照判断模型没有返回思考。

## 验证

2026-09-15：

- 修改前的页面在提供思考正文的浏览器测试中无法直接看到正文；修改后相同条件通过。
- 浏览器模拟数据回归覆盖默认展开、单次折叠、空状态、实时 SSE 片段、长正文滚动、中英文切换、手机宽度、完成状态对齐。所有对话写请求均被模拟或拒绝，没有提交模型请求。
- 69 项时间线投影、进度与过程分组测试通过；前端 Vite 构建通过。新增用例覆盖中途正式输出和尾部答复保留、空思考转正文、多个用户回合、错误／重试、后台完成通知和历史加载时的身份保留。
- 完整 TypeScript 检查仍存在仓库已有错误；与本次修改前日志比较，没有新增诊断类型。
- 真实 FastAPI 对话的思考和工具调用可折叠；各段正式文字和最终答复单独可见。浏览器回归实际发送滚轮事件，验证区域内只滚动过程，区域外滚动对话，并检查手机宽度、双语及实时数据到达后保留手动折叠。

可重跑浏览器回归（在已运行的测试环境选择一个现有开发环境会话，开发环境 ID 与项目 ID 相同）：

```bash
CHAT_TEST_BASE=http://test-host \
CHAT_PROJECT_ID=existing-project-id \
CHAT_SESSION_ID=existing-session-id \
node frontend/test/chat-timeline.browser.cjs
```

运行环境需可导入 Playwright 并安装 Chromium。可以通过 `PLAYWRIGHT_MODULE` 指定模块路径，`CHROME_PATH` 指定浏览器，`CHAT_TEST_OUTPUT` 指定截图目录。测试本地构建预览时，将 `CHAT_TEST_BASE` 指向本地预览，将 `CHAT_UPSTREAM_URL` 指向提供只读初始化数据的测试服务。

## 部署与回滚

本次仅更新 40 的前端 Deployment，不修改 Agent、数据库、渠道或模型设置。

- 镜像：`sealos.hub:5000/opsiforce/opsiforce-frontend:chat-formal-output-20260915-1`
- 已核对运行 Pod 的 digest：`sha256:8caa130f8ce4dc7fd48109299ba3bd6e7bb0c50a16ad75a785c387faa19d3d1f`
- 隔离构建目录：`/root/opsiforce/deploy/.state/chat-formal-output-20260915/source`
- 上级目录保留 `build.log` 和 `frontend-before.yaml`。
- 回滚镜像：`sealos.hub:5000/opsiforce/opsiforce-frontend:chat-activity-20260915-1`。

## 正式输出范围修正

2026-09-15 的后续修正不再只保留最后一个 assistant 的尾部正文。所有 `text` 类型内容均保持在过程折叠之外；浏览器用例同时检查过程前的正式说明与过程后的最终答复可见。
