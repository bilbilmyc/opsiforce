# FastAPI 表单服务发布记录与后续流程

## 当前结果

2026-09-15，项目 `6f1bb441-7519-4a97-b037-b48f64330753` 已完成平台接入、开发预览、首次生产发布和再次发布验证。

- [开发预览](http://6f1bb441-7519-4a97-b037-b48f64330753-dev.apps.opsiforce.12.2.40.40.sslip.io/)
- [生产服务](http://59ee5774-0fad-4b50-8fb4-c80c1fb25749-prod.apps.opsiforce.12.2.40.40.sslip.io/)
- [生产 API 文档](http://59ee5774-0fad-4b50-8fb4-c80c1fb25749-prod.apps.opsiforce.12.2.40.40.sslip.io/docs)

这些 HTTP 地址使用当前 40 的域名入口，已从当前网络实际访问验证。尚未配置自有公网域名和 HTTPS，也未验证任意公网网络到 40 的连通性。

## 补齐的运行环节

原应用仅在容器内 8000 端口运行，平台 3000 端口未启动，且缺少应用登记。已保留 FastAPI 和原生 HTML/CSS/JS，实现：

- 用 `app/startup.sh` 和 `guard app-backend` 托管应用，监听 3000。
- 提供 `/api/app-meta`，登记 `app/app.meta.json`，使平台识别应用并显示发布入口。
- 锁定依赖并使用各环境自己的 Python 虚拟环境。
- 将开发 SQLite 通过 backup 迁移到 `app/data/fastapi.db`，与已有模板数据库分开保存。
- 排除运行数据、虚拟环境和备份，发布仅携带源码；生产首次启动创建独立数据库。

可复用的接入文件与操作说明见 [FastAPI 接入示例](../../deploy/examples/fastapi/README.md)。

## 实际验证

- 开发和生产地址：页面、JS、CSS、API 文档、健康检查和应用登记均返回 200。
- 表单合法提交 201，缺少必填字段 422，删除自己创建的验证记录 204。
- 开发应用通过平台控制端点重启后，记录保留；验证结束保留原有两条记录。
- 首次生产发布完成，生产数据库初始为空，没有复制开发测试数据。
- 在生产创建验证记录后再次发布同一版本，Pod UID 从 `e64d2e67-4749-4015-923b-a570c609e71b` 变为 `4917c991-2e7c-41be-920e-60cc9f5fb8d8`，记录仍在；随后只清理验证记录。

两次发布的应用源码提交都是 `8e5403bc34b13d260e87909a89bae31e3855686f`；发布任务分别为 `97a47044-f6bc-448c-99c9-464c76dd632b` 和 `5cdceb45-aace-4d81-9870-e930e0773e94`。

修改前备份在开发工作区 `.opsiforce/fastapi-publish-20260915-150531`；更早的数据库预备备份也保留。没有覆盖已有模板数据库，没有清空开发数据。

## 后续使用

1. 在开发环境继续改代码。
2. 重启开发应用，通过开发预览检查功能。
3. 顶部环境菜单选择生产目标的“发布更新”。
4. 等待发布成功，再打开生产链接检查。

正式公网接入还需确定自有域名及实际公网入口，配置 DNS、入口转发与 TLS。当前域名直接指向 40；仅购买域名不会替代应用发布和网络入口配置。

生产浏览器端到端验证已通过：填写表单、提交成功、列表显示新记录、表单自动清空、删除验证记录及手机宽度无横向溢出。页面无脚本错误。
