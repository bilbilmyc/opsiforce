{{- if and .Values.ingressRoute.enabled .Values.vscodeProxy.enabled }}
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ include "opsiforce-proxy.fullname" . }}-vscode
  labels:
    {{- include "opsiforce-proxy.labels" . | nindent 4 }}
  annotations:
    external-dns.alpha.kubernetes.io/hostname: "*.{{ .Values.vscodeProxy.appsHostname }}"
    external-dns.alpha.kubernetes.io/target: {{ .Values.ingressRoute.traefikTarget }}
spec:
  entryPoints:
    - websecure
  routes:
    - match: HostRegexp(`{id:[a-f0-9-]+}.{{ .Values.vscodeProxy.appsHostname }}`)
      kind: Rule
      services:
        - name: {{ .Values.vscodeProxy.backendService }}
          port: 3003
  tls:
    certResolver: letsencrypt
    domains:
      - main: "*.{{ .Values.vscodeProxy.appsHostname }}"
{{- end }}
