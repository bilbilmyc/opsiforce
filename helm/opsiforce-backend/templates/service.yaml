apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsiforce-backend.serviceName" . }}
  annotations:
    service.kubernetes.io/topology-mode: "Auto"
  labels:
    app: {{ include "opsiforce-backend.fullname" . }}
spec:
  ports:
    - protocol: TCP
      port: {{ .Values.backend.containerPort }}
      targetPort: {{ .Values.backend.containerPort }}
  selector:
    app: {{ include "opsiforce-backend.fullname" . }}
